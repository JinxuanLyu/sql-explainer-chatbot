[project]
name = "sql-explainer-chatbot"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.110",
  "uvicorn>=0.27",
  "pydantic>=2.6",
  "google-cloud-aiplatform>=1.137.0",
  "litellm>=1.81.13",
]

[tool.uv]
dev-dependencies = []
